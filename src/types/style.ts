export interface GlobalLayoutProps {
  isDark: boolean;
}

export interface MainMenuProps {
  activeIndex: number;
}

export interface CustomInputProps {
  isForm: boolean;
  length: string;
  value: string;
  open?: boolean;
}
